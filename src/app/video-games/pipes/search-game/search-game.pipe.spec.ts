import { SearchGamePipe } from './search-game.pipe';

describe('Search Game Pipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGamePipe();
    expect(pipe).toBeTruthy();
  });
});

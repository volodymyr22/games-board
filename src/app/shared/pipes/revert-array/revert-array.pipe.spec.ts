import { RevertArrayPipe } from './revert-array.pipe';

describe('RevertArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new RevertArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
